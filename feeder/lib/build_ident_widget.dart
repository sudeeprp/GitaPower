import 'package:flutter/material.dart';

class BuildIdentWidget extends StatelessWidget {
  const BuildIdentWidget({super.key});

  @override
  Widget build(context) {
    return Center(child: Text('Version 555'));
  }
}
